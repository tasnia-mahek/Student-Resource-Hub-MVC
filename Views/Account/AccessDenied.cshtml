@{
    ViewData["Title"] = "Access Denied";
}

<section class="auth-page">
    <div class="auth-card auth-message-card">
        <span class="auth-kicker">STUDYHUB ACCOUNT</span>
        <h1>Access denied</h1>
        <p>You do not have permission to view this page.</p>
        <a class="auth-submit auth-submit-link" asp-controller="Home" asp-action="Index">Return home</a>
    </div>
</section>
